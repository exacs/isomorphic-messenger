{
  "name": "isomorphic-messenger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "npm run clean-server && npm run clean-app",
    "build": "npm run build-server && npm run build-app",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean-server": "rm -rf server-es5 && mkdir server-es5",
    "clean-app": "rm -rf app-es5 && mkdir app-es5",
    "build-server": "babel -d ./server-es5 ./server -s",
    "build-app": "babel -d ./app-es5 ./app -s",
    "start": "NODE_PATH=$NODE_PATH:./app-es5 node ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0"
  },
  "dependencies": {
    "express": "^4.13.4",
    "react": "^15.1.0",
    "react-dom": "^15.1.0"
  }
}
